## [0.2.15] - 2026-04-28

Stdlib parse: **1842 / 1842**. The gate is now hard.

### Changed

- **CI stdlib-parse job is now a hard gate.** With 1842/1842 passing
  and zero failures, any regression in the CPython 3.14 stdlib parse
  now blocks a merge. Removed `continue-on-error: true` from the
  `stdlib-parse` job. Also removed the `--allow` flags from the
  unparse check steps that were carrying over from when failures were
  expected.
- CLI version bumped to `0.2.15`.
