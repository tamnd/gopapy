name: release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to build binaries for (e.g. v0.3.4)'
        required: true

env:
  GO_VERSION: '1.26'

permissions:
  contents: write

jobs:
  build:
    name: build ${{ matrix.goos }}/${{ matrix.goarch }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
            bin: gopapy
            archive: tar.gz
          - goos: linux
            goarch: arm64
            bin: gopapy
            archive: tar.gz
          - goos: darwin
            goarch: amd64
            bin: gopapy
            archive: tar.gz
          - goos: darwin
            goarch: arm64
            bin: gopapy
            archive: tar.gz
          - goos: windows
            goarch: amd64
            bin: gopapy.exe
            archive: zip
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.tag || github.ref }}

      - uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Set release tag
        run: echo "RELEASE_TAG=${{ inputs.tag || github.ref_name }}" >> "$GITHUB_ENV"

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: '0'
        run: go build -trimpath -ldflags="-s -w" -o "${{ matrix.bin }}" ./cmd/gopapy

      - name: Stage archive contents
        run: |
          stage=gopapy-${RELEASE_TAG}-${{ matrix.goos }}-${{ matrix.goarch }}
          mkdir -p "$stage"
          cp "${{ matrix.bin }}" "$stage/"
          cp LICENSE README.md CHANGELOG.md "$stage/"
          echo "STAGE=$stage" >> "$GITHUB_ENV"

      - name: Create tarball
        if: matrix.archive == 'tar.gz'
        run: tar -czf "${STAGE}.tar.gz" "$STAGE"

      - name: Create zip
        if: matrix.archive == 'zip'
        run: zip -r "${STAGE}.zip" "$STAGE"

      - uses: actions/upload-artifact@v7
        with:
          name: archive-${{ matrix.goos }}-${{ matrix.goarch }}
          path: gopapy-${{ env.RELEASE_TAG }}-${{ matrix.goos }}-${{ matrix.goarch }}.${{ matrix.archive }}
          if-no-files-found: error
          retention-days: 7

  release:
    name: publish release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.tag || github.ref }}

      - name: Set release tag
        run: echo "RELEASE_TAG=${{ inputs.tag || github.ref_name }}" >> "$GITHUB_ENV"

      - uses: actions/download-artifact@v8
        with:
          path: dist
          pattern: archive-*
          merge-multiple: true

      - name: List artifacts
        run: ls -la dist/

      - uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          files: dist/*
          draft: false
          prerelease: false
