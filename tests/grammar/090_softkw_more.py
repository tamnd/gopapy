from .case import TestCase
from .match import something
from .type import alias

if match := re.search(p, s):
    pass
while case := next_case(it):
    pass
if (type := lookup(name)) is None:
    raise KeyError(name)
